{
  "permissions": {
    "additionalDirectories": ["../on-stage"],
    "deny": [
      "Edit(../on-stage/**)",
      "Write(../on-stage/**)",
      "Bash(rm:../on-stage/**)",
      "Bash(git -C ../on-stage *)"
    ]
  }
}
